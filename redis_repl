#!/usr/bin/perl

use strict;
use warnings;
use Devel::REPL;

my @plugins = (
    'History',
    'RedisConsole',
    #'LexEnv',
);

my $repl = Devel::REPL->new();
$repl->prompt('redis> ');
$repl->load_plugin($_) for (@plugins);
$repl->run();
exit 0;
